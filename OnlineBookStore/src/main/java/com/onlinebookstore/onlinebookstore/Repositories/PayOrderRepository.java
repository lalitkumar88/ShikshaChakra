/*package com.onlinebookstore.onlinebookstore.Repositories;
import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import com.onlinebookstore.onlinebookstore.Entities.PayOrder;

public interface PayOrderRepository extends JpaRepository<PayOrder, Long> {
	
			//fetch all carts which belongs to user=id;
			//	@Query(value="select * from pay_order po where orderno_orderno=: id ")
			//	PayOrder findByOrderno(@Param("id") Long orderno_orderno);
		
}*/